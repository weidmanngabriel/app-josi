name: Temporary Tag Color Sort Repeat

on:
  push:
    branches:
      - codex/tag-color-sort-repeat-20260831

permissions:
  contents: write

jobs:
  apply-build-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: codex/tag-color-sort-repeat-20260831
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply product changes
        run: python .github/scripts/apply-tag-color-sort-repeat.py
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: Commit checked product changes
        run: |
          git config user.name "Gabriel Weidmann"
          git config user.email "gabriel@weidmann.net"
          git add src/App.tsx src/enhancements.css architecture.md concept.md
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Refine tag colors sorting and repeat controls"
          git push origin HEAD:codex/tag-color-sort-repeat-20260831
